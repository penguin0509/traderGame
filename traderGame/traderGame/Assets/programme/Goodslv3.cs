using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Goodslv3 : MonoBehaviour
{
    public static bool CnTF = false;
    public static bool JpTF = false;
    public static bool EsTF = false;
    public static bool UkTF = false;
    public static bool PtTF = false;
    public static bool NlTF = false;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
